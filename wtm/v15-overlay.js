(()=>{
const AGG='https://toolbox-nine-eta.vercel.app/api/wtm-aggregate-v12';
async function refreshAggregate(){try{const r=await fetch(AGG+'?v=15&t='+Date.now(),{cache:'no-store'}),d=await r.json();if(!r.ok)throw Error(d.error||'Aggregate sync failed');state.events=dedupe(d.events||[]);state.statuses=d.statuses||[];state.loading=false;render();}catch(e){console.warn('WTM aggregate refresh failed',e)}}
const prevSource=sourcePage;
sourcePage=function(){const fever=state.statuses.find(s=>s.source==='Fever'),pub=state.statuses.find(s=>s.source==='Public Index'),tm=state.statuses.find(s=>s.source==='Ticketmaster');const rows=state.statuses.filter(s=>!['Fever','Public Index','Ticketmaster'].includes(s.source));return`<section class="sourceSetupV15"><div class="rowHeader"><h2>Source Setup</h2><span>${state.events.length} aggregated events</span></div><div class="setupGridV15"><div class="setupCardV15 success"><div class="setupIcon">🔥</div><div><h3>Fever</h3><p>Automatic public Houston feed. No login or event-by-event importing.</p><div class="setupBadge live">${fever?.status==='live'?`Live · ${fever.count||0} events`:'Syncing…'}</div></div></div><div class="setupCardV15 ${tm?.status==='live'?'success':'needs'}"><div class="setupIcon">🎟️</div><div><h3>Ticketmaster</h3><p>One-time server key. After setup, concerts, sports, theater, venues, images and price ranges sync automatically.</p><div class="setupBadge ${tm?.status==='live'?'live':'partial'}">${tm?.status==='live'?`Live · ${tm.count||0} events`:'Setup required'}</div><button id="tmGuide" class="ghost">Show 3-step setup</button></div></div><div class="setupCardV15 ${pub?.status==='live'?'success':'needs'}"><div class="setupIcon">🧭</div><div><h3>Public Index</h3><p>Finds public Houston pages from POSH, DICE, Bucket Listers, Do713, Houstonia and Total Wine, then normalizes and dedupes them.</p><div class="setupBadge ${pub?.status==='live'?'live':'partial'}">${pub?.status==='live'?`Live · ${pub.count||0} events`:'One-time search API setup'}</div><button id="indexGuide" class="ghost">Show setup</button></div></div><div class="setupCardV15"><div class="setupIcon">✦</div><div><h3>For You profile</h3><p>Your WTM preferences rank events from every source, including public-index POSH results, without needing access to POSH’s private recommendation profile.</p><button id="profileJump" class="ghost">Edit preferences</button></div></div></div><div id="setupGuideBox"></div><details class="statusDetails"><summary>Advanced source health (${rows.length})</summary><div class="sourceList">${rows.map(s=>`<div class="sourceRow"><div><b>${esc(s.source||'Source')}</b><small>${esc(s.mode||s.url||'feed')}</small></div><div class="${statusClass(s)}">${esc(s.status||'unknown')}${s.count!=null?` · ${s.count}`:''}</div></div>`).join('')}</div></details><div class="hierarchyCard"><h3>How WTM fills missing sources</h3><ol><li><b>Primary-source matching:</b> venue, promoter, artist, restaurant, club, Ticketmaster or team schedule first.</li><li><b>Public-index discovery:</b> publicly indexed event pages from POSH/DICE/etc. are discovered automatically and normalized.</li><li><b>Personal attendance:</b> booked/RSVP events can be pulled from your authorized email/calendar once Google OAuth is connected to WTM.</li><li><b>Manual import:</b> last resort only.</li></ol></div></section>`};
const oldBind=bind;bind=function(){oldBind();const box=document.getElementById('setupGuideBox');const t=document.getElementById('tmGuide');if(t)t.onclick=()=>{box.innerHTML=`<div class="guideCard"><button class="guideClose" onclick="this.parentElement.remove()">×</button><h3>Ticketmaster · one-time setup</h3><ol><li>Open <a href="https://developer.ticketmaster.com/products-and-docs/apis/getting-started/" target="_blank" rel="noopener">Ticketmaster Developer</a> and create/sign in to your free developer account.</li><li>Create an app and copy its <b>Consumer Key</b>.</li><li>In Vercel: <b>wtm-whats-the-move → Settings → Environment Variables</b>. Add <code>TICKETMASTER_API_KEY</code> with that key, then redeploy.</li></ol><p>After that, WTM syncs Ticketmaster automatically. You do not paste event links.</p></div>`};const i=document.getElementById('indexGuide');if(i)i.onclick=()=>{box.innerHTML=`<div class="guideCard"><button class="guideClose" onclick="this.parentElement.remove()">×</button><h3>POSH + missing-source index · one-time setup</h3><ol><li>Open <a href="https://brave.com/search/api/" target="_blank" rel="noopener">Brave Search API</a> and create an API key. Brave currently includes monthly free credits.</li><li>In Vercel: <b>toolbox → Settings → Environment Variables</b>. Add <code>BRAVE_SEARCH_API_KEY</code>.</li><li>Redeploy Toolbox. WTM will then search public Houston pages from POSH, DICE, Bucket Listers, Do713, Houstonia and Total Wine on every aggregate refresh.</li></ol><p>The key stays server-side and is never stored in your browser.</p></div>`};const p=document.getElementById('profileJump');if(p)p.onclick=()=>{const det=[...document.querySelectorAll('details')].find(x=>x.textContent.includes('preferences'));if(det)det.open=true;window.scrollTo({top:0,behavior:'smooth'})}};
setTimeout(refreshAggregate,300);
setInterval(refreshAggregate,15*60*1000);
})();